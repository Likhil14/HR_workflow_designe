# HR_workflow_design
build a production-quality HR Workflow Designer as a React artifact. Given the complexity, I have created a fully self-contained, feature-rich implementation using React with all the required patterns.
